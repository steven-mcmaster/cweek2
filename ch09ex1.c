page 49

#include <stdio.h>

struct money {
    int dollars;
    int cents;

};

int main(void) {
    char buf[256];
    int buf_counter = 0;
    while(fgets(buf, sizeof(buf), stdin)) {
        if buf[buf_counter] == '.' {
            buf[0-buf_counter


        }
        
    
    
    }
}

